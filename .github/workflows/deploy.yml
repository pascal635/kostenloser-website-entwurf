name: Deploy Entwürfe (FTP)

# Deployt sites/ per FTPS (lftp) auf dev.likovo.de.
# sites/<kunde>/ landet unter <FTP_SERVER_DIR>/<kunde>/ = dev.likovo.de/<kunde>/
# Kein --delete: es wird nur hochgeladen/aktualisiert, nie serverseitig gelöscht
# (schützt andere Kundenordner, die evtl. schon dort liegen).

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: lftp installieren
        run: sudo apt-get update -qq && sudo apt-get install -y lftp

      - name: FTPS Deploy (lftp)
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_SERVER_DIR: ${{ secrets.FTP_SERVER_DIR }}
        run: |
          umask 077
          printf 'machine %s login %s password %s\n' "$FTP_HOST" "$FTP_USER" "$FTP_PASSWORD" > ~/.netrc
          lftp -e "set ftp:ssl-force true; set ftp:ssl-protect-data true; set ssl:verify-certificate no; set net:timeout 15; set net:max-retries 3; mirror -R --verbose ./sites/ $FTP_SERVER_DIR; bye" "ftp://$FTP_HOST"
